package com.biorecorder.basechart.grouping;


public class #E#GroupingMin extends #E#GroupingFunction {
    private #e# min;

    @Override
    protected void add1(#e# value) {
        if(count == 0) {
            min = value;
        } else {
            min = Math.min(min, value);
        }
        count++;
    }

    @Override
    protected #e#[] groupedValue1() {
        #e#[] groupedValues = {min};
        return groupedValues;
    }
}
